$(document).ready(function(){
    $('.preventpropagation').click(function(event){
        event.stopPropagation();
    });
    //$("#bg-anim").pan({fps: 30, speed: 1, dir: 'left'});
    //$("#bg-anim").find('.span12').pan({fps: 30, speed: 2, dir: 'left'});
});
